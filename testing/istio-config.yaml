---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: istio-config
  namespace: flux-system
spec:
  dependsOn:
    - name: istiod
  interval: 1h
  retryInterval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./istio-config/
  prune: true
  wait: true
  postBuild:
    substitute:
      var_substitution_enabled: "true"
      cluster_name: "home"
